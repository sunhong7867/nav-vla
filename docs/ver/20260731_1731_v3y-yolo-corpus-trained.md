# 2026-07-31 17:31 — v3y: 욜로 시연자 코퍼스 수집 완료, 학습 기동

> 앞선 문서: [14:53 시연자 교체](20260731_1453_yolo-demonstrator-pivot.md)

## 1. `--driver yolo` 접합부 버그 3건 (스모크에서 발견·수정)

1. **`/speed_command` QoS 불일치** — motion_planner 구독이 RELIABLE+TRANSIENT_LOCAL
   (depth 1)인데 기본(VOLATILE) 퍼블리셔는 **조용히** 무시된다. 경고 한 줄이 전부라
   티어 명령이 전달 안 되는 걸 로그(`speed limit: raw=70/250`) 유무로 확인해야 했다.
   navigator와 동일한 command QoS로 교체 (lane/motion/speed 3개 토픽 모두).
2. **순항 에피소드 무출발** — navigator가 기동 시 motion gate("stop")를 걸어두는데,
   존 주행은 navigator의 "start"가 풀지만 순항은 nav_goal이 없어 아무도 안 푼다.
   수집기가 순항 시작 때 `/motion_control_command "start"`를 직접 발행.
3. 정지 순서: 순항 종료는 리코더 먼저, 드라이버 나중 (급정지 프레임 오염 방지 —
   route_oracle 때 규칙 그대로 적용).

스모크 6/6 (존 4 — navigator `arrived: reason=stop_line/area` 정차, 순항 2 — 지정
차선 주행 후 시간 종료). 리셋(순간이동) 직후 욜로 파이프라인 차선 복귀 확인.

## 2. 본 수집 (17:00대 완료)

| | 계획 | 성공 | 실패 |
|---|---|---|---|
| 존 주행 (차선쌍) | 60그룹 120에피소드 → 114 실수집* | 163 합산 | 타임아웃 11 (6.3%) |
| 순항 | 30그룹 60에피소드 | (위에 포함) | |
| heldout (미학습 표현) | 존 8 + 순항 4 = 24 | 22 | 타임아웃 2 |

*링 골 윈도(22–55 m) 밖 시작점 그룹은 계획 단계에서 자동 제외.
타임아웃 = 흔들린 시작점(±0.6 m/±6°)에서 욜로가 차선을 못 찾은 경우 — 시작점
다양화의 대가이며 termination 필터로 학습에서 자동 제외.

## 3. finalize

- heldout 24에피소드를 별도 세션으로 물리 분리 (그룹 접두사 y3h_ 기준, meta.json 판독)
- 리샘플 185/186 수용 (기각 1), heldout 24/24
- **훈련팩 packed_v3y = 173 에피소드** / heldout팩 22
- CMI(차선축, 편향보정) **0.333 bit** — v3r(0.347)과 동급의 언어 신호
- 함정 재발: 세션 자동 선택이 초기 실패 잔재 세션(151108, 3에피소드)을 집었다 —
  **세션 경로는 명시할 것** (수집 세션은 리코더 수명 기준이라 스모크·본수집·heldout이
  한 세션에 공존하고, 실패 잔재 세션이 이웃에 남는다)

## 4. 학습 (진행 중)

```
runs/navvla_smolvla_v3y  GPU 1 (3090)  batch 8 AMP  40K steps
2.15 step/s → 약 5.2시간, 완료 ~22:45 KST.  체크포인트 5K마다
데이터: ~35K 샘플 (hold 패딩은 존 도착 에피소드만 — 종단속도<0.3 m/s 게이트)
```

## 5. 남은 것

- 폐루프 평가 (학습 후): 차선 중앙 주행 확인이 1번 판정 기준 — 오늘의 교체가
  "점선 주행"을 실제로 없앴는가. + 존 정차, 순항, heldout(Qwen 정규화 전제라
  참고 지표), 속도 준수
- 데모 배선: chat_gui(Qwen) → 정규 문장 → vla_bridge. 파서 출력을 정규 문장으로
  변환하는 어댑터 필요 (chat_gui는 현재 /nav_goal JSON을 발행 — VLA 경로용으로
  문장 발행 모드 추가)
- 오늘 오전 라인의 v3(오라클) 데이터·서버 자료는 raw_v3_30hz/packed_v3로 보존만
  (잘못된 경로 선생 — 학습 사용 금지)
