# 2026-08-05 16:40 — v5 데모 정착: 속도 성형(slew), DDS 장애 복구, v6 바깥 재균형 가동

## 1) v5 통합 리허설 — 데모 교체 확정

시뮬 + v5 서버 + 브리지 + navigator + 내레이터 전체 스택:
- 존 좌표 감독: moving → preslow(7.6 m) → **T4 도착·정차** (stop_line)
- 내레이터: 주행 중 한국어 상황 라인 정상 생성

## 2) 속도 문제 — 사용자 관찰의 정량화와 대응

관찰: "직선에서 줄였다 늘렸다, 곡선에서 과감속, 차선 밟음, 전반적으로 느림"

| 측정 | 값 |
|---|---|
| 선생 정상상태 (fast) | 직선 2.69 / 커브 2.66 m/s — **커브에서도 정속** |
| v5 (fast) | 직선 2.48 / 커브 2.35 — 커브 감속 자체는 0.13으로 경미 |
| /cmd_vel 명령 출렁임 | 틱당 \|dv\| p90 **1.22 m/s, 최대 3.2**(0→풀스로틀 점프) |

대응 (vla_bridge_node에 파라미터 2개 추가, 곡률 k=w/v 보존해 w 재계산):
- **`speed_slew 0.08` 채택** (2.4 m/s² 제한): 명령 출렁임 p90 1.22 → **0.08**.
  정지 램프(~1.4 m/s²)와 워치독 비상정지는 영향 없음
- **`speed_scale` 폐기**: 정책이 상태로 자기 속도를 보는 폐루프라 1.18을 곱해도
  평형이 2.48로 되돌아오고(±0.01), 부작용으로 normal이 2.40까지 올라
  **fast/normal 티어 구분이 소멸** — 순손해
- 곡선 과감속으로 보인 것의 상당분은 존 도착 8 m 전 "slowly" 전환(preslow 기능)
- 잔여 격차(fast −8%, 커브 정속성)는 브리지로 불가 → 데이터 측(v6+)

데모 서빙 라인: `vla_bridge_node -p max_speed:=3.2 -p image_topic:=/camera/image_raw -p speed_slew:=0.08`

## 3) "차가 안 움직여요" — DDS 그래프 붕괴

GUI 무반응의 원인은 모델이 아니라 통신:
- 1차: GUI가 기본 `control_backend=qwen`(욜로 스택 경로)으로 떠서 `/motion_control_command`에
  디스패치 — 욜로 노드 부재로 무시. **smolvla 모드는 `-p control_backend:=smolvla` 필수**
- 2차: 그 후 CLI pub조차 "no matching subscription" — 하루 수십 번의 노드 재시작으로
  DDS 참여자 그래프가 꼬임 (기존 노드는 동작, 신규 참여자만 디스커버리 실패).
  **전 노드 kill + `/dev/shm`의 fastdds/cyclone 잔재 삭제 + 전체 재기동**으로 해결
- 재발 시 처방전: demo_up.sh (클린 재기동 + 주행 자가진단 포함)

## 4) v6 바깥 차선 재균형 — 수집·학습 가동

v5의 유일한 후퇴(바깥 0.19→0.29 m) 대응. `collect_corpus.py`에 `--cruise-lane` 추가:
- **lane2 전용 크루즈 36 에피소드** 수집 (35 성공, 20분)
- **off-ring 가드 첫 실전 포착 1건** (lane2에서도 이탈 발생 → 실패 처리·제외).
  어제의 오염 경로가 원천 차단됨을 확인
- packed_v6 = v5(372) + y6o(34) = **406 eps / 93.7K 프레임** → `navvla_smolvla_v6`
  60K 학습 중 (완료 ~01:10)

## 미해결

- 내일: v6 링 맵 평가 — 바깥 0.19 복원 + 안쪽 0.41~0.55 유지 + 이탈 0이면 최종 교체
- 커브 정속성·fast 상한은 코퍼스가 늘며 개선되는지 관찰 (안 되면 fast 비중 조정)
- 실차 전이 설계 (라이다 위치추정 어댑터) — 사용자와 논의 예정
