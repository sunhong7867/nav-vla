# 2026-08-04 00:30 — v4 병합 재학습: 안쪽 개선, 그러나 트랙 이탈 불안정 → 데모는 v3y-40K 유지

## 무엇을 했나

- 취약 구간(안쪽 차선 절반, S-커브, M3 커브) 겨냥 보충 수집 167 에피소드를
  기존 v3y 코퍼스 173에피소드와 병합 (총 340 eps, ~78.6K 프레임 @10Hz)
- 병합 코퍼스로 SmolVLA 60K 스텝 재학습 (`navvla_smolvla_v4`, 8h40m, 최종 loss 0.047, 6.1 epoch)
- 30K/50K/60K 체크포인트를 동일 링 맵 프로브(안/밖 115초 크루즈, 시작 (-1.49, 24.55))로 평가,
  v3y-40K 구간 맵과 비교

## 결과

| 체크포인트 | 결과 |
|---|---|
| v4-30K | S-커브에서 매번 이탈, 완주 불가 (사용자 실시간 확인) |
| v4-50K | 4회 주행 전부 트랙 이탈, 전부 우상단 밖 유사 지점으로 발산 |
| v4-60K | 4회 중 3회 이탈. 완주한 안쪽 주행은 크게 개선: 중앙값 \|편차\| 0.76→0.43 m, 최대 2.68→1.46 m, 0.5 m 초과 구간 13→8. 바깥은 첫 바퀴 완주 후 둘째 바퀴 T2–M3에서 이탈 |

핵심: **개선 신호는 진짜 있으나(보충 데이터가 겨냥한 안쪽 구간이 실제로 좋아짐),
주행이 확률적으로 무너져 데모 불가.** 기준선 v3y-40K는 편차는 커도 이탈은 없음.

## 원인 분석

- 데이터 감사는 깨끗: 두 그룹 모두 `/camera/image_raw`, termination 전부 success,
  dropped 0, tier/차선/과업 분포 균형
- 남는 유력 가설은 **학습 설정**: v3y-40K는 34.6K 프레임에 40K 스텝(~9.2 epoch)으로 안정,
  v4는 데이터가 2.3배인데 60K 스텝 — lr 스케줄과 에포크 구성이 달라짐
- 검증 실험으로 **v4b** 가동: 동일 병합 데이터 + 검증된 v3y 레시피(40K 스텝)
  (`navvla_smolvla_v4b`, 00:24 시작, 완료 ~06:10). 40K도 불안정하면 보충 데이터 쪽 문제로 전환

## 결정

- 데모/서빙 체크포인트는 **v3y-40K 유지** (`ckpt_v3y40`)
- v4 30K/50K 로컬 사본 삭제, 60K은 분석용으로 로컬 보관
- 아침에 v4b-40K 링 맵 평가 후 교체 여부 재판단
