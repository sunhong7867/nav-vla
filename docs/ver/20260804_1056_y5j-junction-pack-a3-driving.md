# 2026-08-04 10:56 — 분기점 팩 수집(y5j) + 주행판 A3: 미학습 표현은 여전히 경계 밖

오전 작업 두 건. v4c(80K, 에포크 등가) 학습은 서버에서 진행 중 (~18:00).

## 1) 분기점 겨냥 수집 y5j — 48/48 성공, 31분

v4/v4b의 이탈 6건이 전부 OUT 출구 분기점(우측, 코퍼스의 0.6% 최희소 프레임)과
좌상단 코너에 몰린 것에 대한 대비책. `collect_corpus.py`에 `--start-windows` 옵션
추가 (크루즈 시작 인덱스를 지정 구간으로 제한):

```bash
collect_corpus.py --driver yolo --cruise-groups 24 \
  --start-windows "120-180,290-350" --group-prefix y5j_ --seed 20261201
```

- 120–180 = OUT 분기점 상류(우하단), 290–350 = 좌상단 코너 상류 — 모든 크루즈가
  두 취약 지점 중 하나를 통과
- 48 eps 전부 success → resample → `packed_y5j`로 서버 이송 (병합 안 함 —
  v4c 평가 후 결정). 로컬 원본은 파일 수 대조(30,520=30,520) 후 삭제

## 2) 주행판 A3 (v3y-40K): 전-헬드아웃 문장 반사실

07-30 주차판 A3(0/8)의 주행 버전. 템플릿·차선어·속도어 전부 heldout 분할
("Roam the track in the innermost lane, at a good clip." 등), 정준 출발점 75초.

| 반사실 | 분리 (중앙값 횡차, 설계 3.16 m) | 판정 |
|---|---|---|
| 학습 표현 (기준선) | **+2.51 m** (SAME 노이즈 +0.05) | 분기함 |
| 헬드아웃 1 (innermost/outermost) | +0.36 m | 분기 실패 |
| 헬드아웃 2 (closer to centre/edge) | +0.06 m | 분기 실패 |
| 헬드아웃 속도 (taking it easy / in a hurry) | 2.33 vs 2.31 m/s | 대비 없음 |

**어휘 확대(표면형 6개/슬롯)로도 미학습 패러프레이즈는 액션까지 연결되지 않았다**
— 주차판 A3와 같은 결론이 주행에서도 재현. 단, 붕괴 형태는 양호: 미지 표현에서도
링 이탈 없이 기본 크루즈를 유지(75초 완주). "모르면 안전한 기본 주행"은 데모에
바람직한 실패 모드.

### 함의

- **Qwen 파서 하이브리드가 정당화됨**: 자유 문장 → 정규 학습 문장 변환은 파서가
  맡고, VLA는 학습 문장에서만 확실히 분기 — 현 아키텍처 그대로가 맞다
- 출하 주장에서 "미학습 패러프레이즈 일반화" 제외 유지 (07-30과 동일)
- 표면형 6개도 부족하다는 데이터 포인트 — 진짜 일반화를 원하면 표면형 수십 개
  또는 백본 언어공간 보존(레이어 축소 완화) 실험이 필요

## 재현

```bash
# 스크래치패드: a3_heldout_v3y.sh (프로브 3쌍) → a3_analyze.py
```

## 정리

- A3 종료 후 시뮬·서버·브리지 전부 다운 확인, gz 로그 삭제 (로컬 여유 36 GB)
- 다음 이벤트: v4c-80K 워처 (~18:00) → 링 맵 평가 → 이탈 0이면 데모 교체 검토
